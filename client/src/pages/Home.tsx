import React from 'react';
import {Calendar,Navbar} from '../components'

const Home = ({}) => {
    return(
        <div>
            <Navbar/>
            <Calendar/>
        </div>
    );
}

export default Home;