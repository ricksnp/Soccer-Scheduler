import React from 'react';
import {Calendar,TopNav} from '../components'

const Home = ({}) => {
    return(
        <div>
            <TopNav/>
            <Calendar/>
        </div>
    );
}

export default Home;